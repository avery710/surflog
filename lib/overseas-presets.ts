/**
 * Free-text "custom:" spot presets for when Capy is travelling — Swelleye
 * only covers Taiwan (see CLAUDE.md "The automation problem"). These are
 * convenience options in the spot picker only; they carry no coordinates,
 * so condOpenMeteo stays unfilled for them until real lat/lng is confirmed
 * and added (never approximate a coordinate — CLAUDE.md "Spots").
 */
export const OVERSEAS_PRESETS = [
  "Siargao - Cloud 9",
  "Siargao - Quicksilver",
  "Siargao - Stimpy's",
  "Somewhere else",
];
